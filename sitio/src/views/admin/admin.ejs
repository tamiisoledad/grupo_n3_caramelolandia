<!DOCTYPE html>
<html lang="es">

<%- include('../partials/head', {title: "Administrador"})%>

<body>
    <%- include('../partials/header') %>
    <main id="admin">
        <section>
            <h1>Productos</h1>
            <table id="table">
                <thead>
                    <tr>
                        <th><buscador></buscador></th>
                        <th></th>
                        <th><a href="/products/add" id="create"><i class="icon">add</i><a></th>
                        <th>PRECIO</th>
                        <th>DESCUENTO</th>
                        <th>STOCK</th>
                    </tr>
                </thead>

                <% productos.forEach(producto => { %>
                    <tr>
                        <td><a href="/products/detail/<%= producto.id %>"><%= producto.categoria %> <%= producto.Marca %> de <%= producto.Variedad %></a></td>
                        <td>
                            <% if(producto.Vegano){ %>
                                <img class="tag" src="images/celiacos-1.jpg">
                            <% } %>
                            <% if(producto.Celiaco){ %>
                                <img class="tag" src="images/descarga.png">
                            <% } %>
                        </td>
                        <td>
                            <a href="/products/edit/<%= producto.id %>" class="update"><i class="icon">edit</i></a>

                            <form
                                action="/products/delete/<%= producto.id %>?_method=DELETE"
                                method="POST">
                                <button type='submit' class="release"><i class="icon">delete</i></button>
                            </form>
                        </td>
                        <td><%= producto.Precio %></td>
                        <td><%= producto.Descuento %></td>
                        <td><%= producto.Stock %></td>
                    </tr>
                <% }) %>
            </table>
        </section>
    </main>
    <%- include('../partials/footer') %>

</body>
<script>
const $ = x => document.querySelectorAll(x);

$('buscador')[0].innerHTML = `
    <input
        type="search"
        id="buscador"
        oninput="search()"
        placeholder="Alfajor, caramelo..."
    >
`
function search() {
    let input, filter, table, tr, td, i, txtValue;
    input = $("#buscador")[0];
    filter = input.value.toUpperCase();
    table = $("#table")[0];
    tr = table.getElementsByTagName("tr");
    for (i = 0; i < tr.length; i++) {
        td = tr[i].getElementsByTagName("td")[0];
        if (td) {
            txtValue = td.textContent || td.innerText;
            if (txtValue.toUpperCase().indexOf(filter) > -1) {
            tr[i].style.display = "";
            } else {
                tr[i].style.display = "none";
            }
        }
    }
}
</script>
</html>